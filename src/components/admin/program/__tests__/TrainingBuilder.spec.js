import { describe, it, expect } from 'vitest'
import TrainingBuilder from '../TrainingBuilder.vue'

describe('TrainingBuilder', () => {
  it('exports correctly', () => {
    expect(TrainingBuilder).toBeDefined()
  })
})
