import { describe, it, expect } from 'vitest'
import MobileCard from '../MobileCard.vue'

/**
 * Tests for MobileCard Component
 */
describe('MobileCard', () => {
  it('exports component correctly', () => {
    expect(MobileCard).toBeDefined()
  })
})
